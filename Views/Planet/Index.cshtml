@inject App.Services.PlanetService planetService;

<h1>Hệ mặt trời</h1>

<div class="list-group">

    @foreach (var p in planetService)
    {
        <a class="list-group-item list-group-item-action" asp-action="@p.Name">
            @p.Name - @p.VnName
        </a>
    }
</div>
